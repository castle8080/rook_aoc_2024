#ifndef __IO_UTILS_HPP__
#define __IO_UTILS_HPP__

#include <string>
#include <iostream>
#include <vector>

namespace rook::aoc {

    std::vector<std::string> ioReadLines(const std::string& fileName);

}

#endif